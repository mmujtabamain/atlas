[package]
name = "atlas-workspace"
description = "The workspace layout model of Atlas Financer: windows, splits, stacks and panes as pure data — operations, invariants, history, persistence and saved layouts, with no UI dependency."
version.workspace = true
edition.workspace = true
publish.workspace = true
license.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
log.workspace = true
chrono.workspace = true

[dev-dependencies]
tempfile = "3"
